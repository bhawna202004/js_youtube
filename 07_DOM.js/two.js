// console queries to change in webpage

/*
const tempList = document.querySelectorAll('li')
console.log(tempList);

tempList[0].style.Color = 'blue'
tempList[1].style.color = 'green'

tempList.forEach( function(li) {
    li.style.backgroundColor = 'red'
})

document.getElementsByClassName('list-item')

const tempClass = document.getElementsByClassName('list-item') // no loop allow

// convert into  array to use loop function

const myConvertedArr = Array.from(tempClass)

myConvertedArr.forEach( function (lists) {
    lists.style.color = 'magenta'
})  */